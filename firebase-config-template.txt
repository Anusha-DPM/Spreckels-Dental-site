# Copy this content to your .env.local file
# Replace the values with your actual Firebase project configuration

NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key-here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Instructions:
# 1. Create a file named .env.local in your project root
# 2. Copy the above content into it
# 3. Replace the placeholder values with your actual Firebase config
# 4. Get these values from your Firebase Console > Project Settings > General > Your Apps > Web App 